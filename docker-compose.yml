services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: event_tracker_api
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
    ports:
      - "${PORT:-5000}:5000"
